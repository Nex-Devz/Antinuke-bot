AntiN8 — Discord Anti-Nuke Bot Master Prompt

Project

Build a production-ready Discord Anti-Nuke and Anti-Abuse bot named:

AntiN8

Developer:

Zynrax Development

The bot must be designed for real-world Discord servers, including large communities.

Do not create a proof of concept.

Do not create placeholder implementations for core security functionality.

Implement the complete architecture.

---

Technology Stack

Use:

- Node.js
- JavaScript
- Discord.js
- SQLite
- better-sqlite3
- Native in-memory caching
- Discord Gateway
- Discord REST API only when required
- Discord Audit Logs when executor identification is necessary

Do NOT use:

- TypeScript
- Redis
- MongoDB
- PostgreSQL
- Prisma
- Sequelize
- Any external caching database

Use CommonJS or ESM consistently throughout the project.

Prefer modern ESM if the project is configured with:

{
    "type": "module"
}

---

Logging

Use "console.log", "console.warn", and "console.error".

Do not add a logging framework unless absolutely required.

Examples:

console.log("[AntiN8] Client ready");
console.log("[Security] Dangerous role invite detected");
console.warn("[Security] Audit executor could not be resolved");
console.error("[Database] SQLite error:", error);

Keep console output clean and useful.

Use prefixes:

[AntiN8]
[Security]
[Database]
[Gateway]
[Cache]
[Recovery]

---

Source Code

Do not add comments to source code.

Do not use:

// Check role
// Get executor
/* Security check */

Code must be self-explanatory through:

- Good variable names
- Good function names
- Modular files
- Clear services

---

Core Security Modules

Implement:

1. Anti Channel Nuke
2. Anti Role Nuke
3. Anti Permission Nuke
4. Anti Webhook Nuke
5. Anti Emoji Nuke
6. Anti Sticker Nuke
7. Anti Ban
8. Anti Kick
9. Anti Member Role Abuse
10. Anti Administrator Escalation
11. Anti Bot Add
12. Anti Integration Abuse
13. Anti AutoMod Abuse
14. Anti Scheduled Event Abuse
15. Anti Invite Nuke
16. Anti Invite Role
17. Anti Linked Role
18. Anti Raid
19. Anti Mass Mention Abuse
20. Emergency Lockdown

Every module must be independently configurable.

---

Gateway First

AntiN8 must primarily use Discord Gateway events.

Never continuously poll Discord.

Use Gateway events for detection whenever available.

Examples:

Role Create
Role Update
Role Delete

Channel Create
Channel Update
Channel Delete

Guild Member Add
Guild Member Remove
Guild Member Update

Invite Create
Invite Delete

Webhooks Update

Guild Ban Add
Guild Ban Remove

Guild Update

Integration Update

AutoMod Rule Create
AutoMod Rule Update
AutoMod Rule Delete

Scheduled Event Create
Scheduled Event Update
Scheduled Event Delete

Use the events actually supported by the installed Discord.js version.

---

API Request Philosophy

Follow:

Gateway Event
      ↓
Memory Cache
      ↓
Local Security Check
      ↓
Only if required
      ↓
Targeted Discord API request

Never do:

Gateway Event
      ↓
Fetch everything
      ↓
Fetch Audit Logs
      ↓
Fetch Guild
      ↓
Fetch Roles
      ↓
Fetch Channels

unless genuinely necessary.

---

In-Memory Cache

Use native JavaScript:

Map
Set
WeakMap

No Redis.

Global structure:

const guildCache = new Map();

Example:

{
    config,
    whitelist,
    extraOwners,
    protectedRoles,
    protectedChannels,
    protectedWebhooks,
    roleSnapshots,
    channelSnapshots,
    inviteCache,
    rateLimits,
    auditCache,
    dedupeCache,
    linkedRoles
}

---

Cache Priority

Every security check should follow:

Memory
 ↓
SQLite
 ↓
Discord API

Memory must handle hot-path security checks.

Do not execute SQLite queries for every Gateway event.

Do not execute REST requests for every event.

---

SQLite

Use:

better-sqlite3

Database:

data/antin8.sqlite

Enable:

PRAGMA journal_mode = WAL;
PRAGMA foreign_keys = ON;

Use prepared statements.

Use transactions for multi-step writes.

---

SQLite Tables

Create migrations for:

guilds
security_config
whitelist
extra_owners
protected_roles
protected_channels
protected_webhooks
trusted_bots
security_incidents
security_snapshots
punishment_history
invite_history

Use indexes on frequently searched fields.

---

Persistent vs Memory Data

SQLite stores:

Guild configuration
Whitelist
Extra Owners
Protected resources
Security incidents
Punishment history
Snapshots required for recovery
Invite history

Memory stores:

Rate limits
Recent events
Invite state
Audit resolutions
Snapshots during runtime
Deduplication state
Cached configuration
Cached whitelist
Cached owners

Do not persist every Gateway event.

---

Anti Channel Nuke

Detect:

- Channel deletion
- Channel creation spam
- Channel updates
- Permission overwrite destruction
- Permission overwrite creation
- Permission overwrite modification
- Channel position manipulation
- Channel type manipulation
- Mass channel deletion
- Mass channel creation

Maintain channel snapshots.

For unauthorized deletion:

Detect
↓
Resolve executor
↓
Whitelist check
↓
Extra Owner check
↓
Risk calculation
↓
Punishment
↓
Restore
↓
Incident log

---

Anti Role Nuke

Detect:

- Role deletion
- Role creation spam
- Role updates
- Permission escalation
- Role position manipulation
- Mass role deletion
- Mass role creation
- Mentionable changes
- Hoist changes
- Role icon changes
- Unicode emoji changes

Maintain role snapshots.

---

Dangerous Permission Detection

Never detect dangerous roles by their name.

Never use:

role.name.includes("admin")

Security decisions must use actual Discord permission flags.

Critical:

PermissionFlagsBits.Administrator

High-risk:

PermissionFlagsBits.ManageGuild
PermissionFlagsBits.ManageRoles
PermissionFlagsBits.ManageChannels
PermissionFlagsBits.ManageWebhooks
PermissionFlagsBits.BanMembers
PermissionFlagsBits.KickMembers

Medium-risk:

PermissionFlagsBits.ModerateMembers
PermissionFlagsBits.ManageMessages
PermissionFlagsBits.ManageThreads
PermissionFlagsBits.ManageNicknames
PermissionFlagsBits.ManageEvents
PermissionFlagsBits.MentionEveryone

The exact risk list must be centralized in a PermissionAnalyzer service.

---

Permission Risk Engine

Create:

src/security/PermissionAnalyzer.js

Example:

import { PermissionFlagsBits } from "discord.js";

const permissionWeights = new Map([
    [PermissionFlagsBits.Administrator, 100],
    [PermissionFlagsBits.ManageGuild, 90],
    [PermissionFlagsBits.ManageRoles, 90],
    [PermissionFlagsBits.ManageChannels, 80],
    [PermissionFlagsBits.ManageWebhooks, 70],
    [PermissionFlagsBits.BanMembers, 70],
    [PermissionFlagsBits.KickMembers, 60],
    [PermissionFlagsBits.ModerateMembers, 50],
    [PermissionFlagsBits.ManageMessages, 40],
    [PermissionFlagsBits.MentionEveryone, 30]
]);

export function getRoleRisk(role) {
    if (role.permissions.has(PermissionFlagsBits.Administrator)) {
        return 100;
    }

    let highestRisk = 0;

    for (const [permission, weight] of permissionWeights) {
        if (role.permissions.has(permission)) {
            highestRisk = Math.max(highestRisk, weight);
        }
    }

    return highestRisk;
}

Use the highest dangerous permission rather than blindly adding all permission weights.

---

Anti Invite Role

This is a dedicated module.

Discord invites can contain role IDs.

Use:

invite.roleIds

directly from the Gateway event.

Do not fetch the invite again.

Flow:

INVITE_CREATE
      ↓
roleIds.length?
      ↓
No → Ignore
      ↓
Yes
      ↓
Resolve roles from guild.roles.cache
      ↓
Inspect permissions
      ↓
Calculate risk
      ↓
Protected role check
      ↓
Whitelist check
      ↓
Extra Owner check
      ↓
Action

---

Invite Role Risk

Example:

Administrator       100
Manage Roles         90
Manage Guild         90
Manage Channels      80
Manage Webhooks      70
Ban Members           70
Kick Members          60
Moderate Members      50
Manage Messages       40
Mention Everyone      30
Normal role            0

Make thresholds configurable.

---

Invite Role Actions

Default:

0–29
ALLOW

30–69
LOG

70–89
DELETE INVITE

90–100
DELETE INVITE
+
AUDIT CORRELATION
+
PUNISH
+
CRITICAL LOG

Trusted users may bypass the configured action.

Never punish the creator merely because an invite contains a harmless role.

---

Invite Role Example

Attack:

Attacker
↓
Creates invite
↓
Invite contains @Administrator
↓
AntiN8 receives INVITE_CREATE
↓
roleIds detected
↓
Role resolved
↓
Administrator permission detected
↓
Risk = 100
↓
Creator not trusted
↓
Invite deleted
↓
Executor resolved
↓
Punishment applied
↓
Security incident stored

---

Anti Linked Role

Create:

src/modules/antilinkedrole/

Detect Discord Linked Roles through role metadata/tags.

Maintain a memory set:

linkedRoleIds = new Set();

Protect:

- Linked role deletion
- Linked role modification
- Permission escalation
- Hierarchy manipulation
- Unauthorized linked-role destruction

Do not assume Linked Role requirements are ordinary Role properties.

Protect the actual Discord role and supported metadata.

---

Linked Role Cache

At startup and whenever relevant Gateway events occur:

Guild Create
Role Create
Role Update

update the linked-role cache.

Do not repeatedly fetch guild roles from REST.

Use:

guild.roles.cache

first.

---

Anti Webhook

Detect:

- Webhook creation
- Webhook deletion
- Webhook modification
- Mass webhook creation
- Mass webhook deletion

Delete malicious webhooks when manageable.

---

Anti Ban

Detect:

5 bans / 10 seconds
10 bans / 30 seconds
20 bans / 60 seconds

Make thresholds configurable.

Use memory counters.

Do not query SQLite to count events.

---

Anti Kick

Same architecture as Anti Ban.

Use in-memory counters.

---

Anti Member Role Abuse

Detect:

- Mass role assignment
- Mass role removal
- Dangerous role assignment
- Administrator role assignment
- Moderator role assignment
- Protected role assignment

Track:

Executor
Target
Role
Timestamp

Detect rapid privilege escalation.

---

Anti Bot Add

Detect unauthorized bots joining.

Check:

Bot ID
Inviter
Whitelist
Extra Owner
Trusted bot list

Support:

Allowed Bots
Blocked Bots
Trusted Bots

---

Anti Invite

Protect:

- Invite creation spam
- Invite deletion spam
- Unauthorized invite deletion
- Invite flood

Maintain invite cache:

Map<GuildId, Map<InviteCode, InviteState>>

Example:

{
    code,
    guildId,
    channelId,
    inviterId,
    roleIds,
    uses,
    maxUses,
    maxAge,
    createdAt
}

---

Invite Tracking

Do not fetch all invites on every member join.

Use:

INVITE_CREATE
↓
Update cache

INVITE_DELETE
↓
Remove cache

GUILD_MEMBER_ADD
↓
Check cached invite state
↓
Only reconcile with Discord if required

If invite identification is impossible from current state, perform a targeted reconciliation.

---

Audit Log Fetching

Do not fetch audit logs for every event.

Only fetch audit logs when:

- Executor isn't available from the event
- Action is security-sensitive
- Risk threshold is reached
- Executor identity is required for enforcement

Example:

Normal role update
↓
No dangerous change
↓
No audit request

Dangerous permission escalation
↓
Audit correlation required
↓
Fetch relevant audit entries

Cache recent audit resolutions in memory.

---

Audit Correlation

Match:

Action
Target ID
Timestamp
Executor

Use a short correlation window.

If executor cannot be reliably identified:

executorId = null;

Never punish a random administrator.

---

Whitelist System

Implement granular whitelist support.

Whitelist:

- Users
- Roles
- Bots
- Modules
- Actions

Example:

User A
→ ROLE_UPDATE
→ INVITE_CREATE

User B
→ ALL

Database:

{
    guildId,
    targetId,
    targetType,
    actions,
    addedBy,
    createdAt
}

---

Extra Owner System

Support multiple Extra Owners.

Commands:

/security owner add @user
/security owner remove @user
/security owner list

Extra Owners can be trusted for Anti-Nuke operations.

Store in SQLite and load into memory.

Do not hardcode Extra Owners.

---

Trust Priority

Use:

Guild Owner
↓
Extra Owner
↓
Explicit Trusted User
↓
Whitelisted User/Role
↓
Normal User

Make bypass rules configurable.

---

Punishment Engine

Create:

src/security/PunishmentEngine.js

Supported actions:

BAN
KICK
TIMEOUT
STRIP_ROLES
REMOVE_DANGEROUS_ROLE
QUARANTINE
NONE

Default severity:

LOW
LOG

MEDIUM
STRIP_ROLES

HIGH
KICK

CRITICAL
BAN

Never blindly ban.

Check hierarchy before punishment.

---

Rollback

Create:

src/security/SnapshotManager.js

Support rollback for manageable resources.

Examples:

Roles
Channels
Permission Overwrites
Webhooks
Emojis
Stickers
Supported guild settings

Never claim rollback succeeded unless Discord confirms the operation.

---

Self Protection

AntiN8 must never punish:

- Itself
- Guild owner
- Protected internal accounts

Before destructive actions verify:

Bot permission
+
Role hierarchy
+
Target manageability
+
Target isn't guild owner
+
Target isn't AntiN8

---

Lockdown

Implement:

/security lockdown
/security unlock

Lockdown should increase security sensitivity.

During lockdown:

Dangerous role invites → Block
Suspicious role changes → Block
Suspicious channel changes → Block
Suspicious bot additions → Block
Security logging → Increased

Do not blindly block every normal server action.

---

Incident Engine

Create:

src/security/IncidentEngine.js

Every serious security event becomes an incident.

Structure:

{
    id,
    guildId,
    module,
    action,
    executorId,
    targetId,
    severity,
    risk,
    evidence,
    actionTaken,
    createdAt
}

Use SQLite for persistent incident history.

---

Rate Limiter

Use only memory.

Example:

const rateLimits = new Map();

Never use SQLite for high-frequency counters.

Clean expired entries automatically.

---

Event Deduplication

Use memory:

const dedupeCache = new Map();

Prevent duplicate processing during event bursts.

---

Database Access Pattern

Hot path:

Gateway
↓
Memory
↓
Decision

Cold path:

Memory miss
↓
SQLite
↓
Update Memory

External path:

Only when necessary
↓
Discord API

---

UI

All bot-facing UI must use Discord Components V2.

Do not use legacy embeds for the dashboard.

Use:

- Container
- Text Display
- Separator
- Section
- Button
- String Select
- User Select
- Role Select
- Channel Select
- Mentionable Select
- Media Gallery where useful

---

UI Style

Pure clean UI.

No emojis.

No decorative emoji status indicators.

No ASCII art.

No unnecessary colors.

No huge blocks of text.

No legacy embed dashboard.

Use:

Container
Text Display
Separator
Section
Buttons
Select Menus

Example:

# AntiN8 Security

Server protection is active.

────────────────

Protection

Anti-Nuke       Enabled
Invite Role     Enabled
Linked Role     Enabled
Anti-Raid       Enabled

────────────────

Threat Level

Low

────────────────

Protected Resources

Roles       14
Channels    28
Webhooks     6

────────────────

[Configure] [Whitelist] [Logs]

Implement this using Components V2, not an Embed.

---

Media Gallery

Media Gallery is optional.

Use it only where it improves the UI.

Possible uses:

- Security overview
- Incident evidence
- AntiN8 branding

Do not use Media Gallery simply to decorate every page.

---

Dashboard Pages

Main:

Overview
Protection
Whitelist
Extra Owners
Thresholds
Punishments
Incidents
Logs
Settings

Protection:

Anti Channel
Anti Role
Anti Permission
Anti Webhook
Anti Ban
Anti Kick
Anti Bot
Anti Invite
Anti Invite Role
Anti Linked Role
Anti Raid

Use buttons/select menus for navigation.

---

Commands

Implement:

/security
/security setup
/security status
/security config

/security whitelist add
/security whitelist remove
/security whitelist list

/security owner add
/security owner remove
/security owner list

/security protection
/security thresholds
/security punishments

/security incidents
/security logs

/security lockdown
/security unlock

Use proper slash-command permissions.

---

Project Structure

Use:

src/
├── commands/
├── events/
├── components/
├── modules/
│   ├── antichannel/
│   ├── antir ole/
│   ├── antipermission/
│   ├── antiwebhook/
│   ├── antiban/
│   ├── antikick/
│   ├── antimemberrole/
│   ├── antibot/
│   ├── antiintegration/
│   ├── antiautomod/
│   ├── antischeduledevent/
│   ├── antiinvite/
│   ├── antiinvite-role/
│   ├── antilinked-role/
│   └── antiraid/
├── security/
│   ├── RiskEngine.js
│   ├── PermissionAnalyzer.js
│   ├── IncidentEngine.js
│   ├── AuditCorrelator.js
│   ├── PunishmentEngine.js
│   ├── SnapshotManager.js
│   ├── WhitelistManager.js
│   └── OwnerManager.js
├── database/
│   ├── Database.js
│   ├── migrations/
│   └── repositories/
├── cache/
│   └── GuildCache.js
├── utils/
├── config/
└── index.js

Correct any typo in directory names when implementing.

---

Startup

Startup flow:

Process Start
↓
Initialize SQLite
↓
Run migrations
↓
Create Discord Client
↓
Register event handlers
↓
Login
↓
Gateway Ready
↓
Load guild configurations
↓
Build memory caches
↓
Build snapshots from available Discord.js cache
↓
Initialize invite state where required
↓
Enable protection

Do not perform unnecessary REST synchronization.

---

Recovery

After restart:

SQLite
↓
Load configuration
↓
Memory cache
↓
Gateway state
↓
Rebuild runtime snapshots
↓
Resume protection

The bot must remain functional after restarts.

---

Error Handling

Handle:

403
404
429
500+
Missing permissions
Role hierarchy failures
Missing resources
Audit log unavailable
Database failures
Gateway reconnects

Never crash the entire bot because one guild operation failed.

Use:

console.error(...)

with useful context.

---

Performance

The bot must be optimized for large servers.

Avoid:

REST request per event
Audit request per event
SQLite query per event
Invite fetch per join
Full guild fetch loops
Continuous polling

Prefer:

Gateway
↓
Memory
↓
Local calculation
↓
Targeted API only when required

---

No Redis

The project must contain zero Redis usage.

Do not install:

redis
ioredis

Do not create Redis-related files.

---

No TypeScript

Do not create:

.ts
.tsx

Use:

.js
.json
.sql

Only.

---

No Comments

Do not add comments to implementation files.

Use clear naming and modular architecture instead.

---

Final Requirement

AntiN8 must function as a complete security platform.

The final system must combine:

Gateway Detection
+
Memory Cache
+
SQLite Persistence
+
Risk Analysis
+
Permission Analysis
+
Whitelist
+
Extra Owners
+
Audit Correlation
+
Punishment
+
Rollback
+
Incident Management
+
Lockdown
+
Components V2 UI

The primary optimization principle is:

GATEWAY FIRST
MEMORY FIRST
SQLITE SECOND
DISCORD API LAST

The bot should perform the minimum number of Discord API requests necessary to make a reliable security decision.

Do not sacrifice security for fewer requests when authoritative Discord data is genuinely required.

Build the project as production software, not tutorial code.
